spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/querydsl
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    show-sql: true  # ????? ?????
    hibernate:
      ddl-auto: create # ?????? ?? ? ??? ?? -> ??, ?????? ???? ??? ??
    properties:
      hibernate:
        #        show_sql: true # JPA ?? ? ???? sql ?? ??? ??? -> org.hibernate.SQL: debug ? ??
        format_sql: true
        highlight_sql: true # ANSI ??? ?? ????
        use_sql_comments: true # ???? JPQL? ? ? ??. SQL ??? /* */ ?? ??
  logging.level:
    org.hibernate.SQL: debug # ??? JPA ? ???? sql ? ??.
  #     org.hibernate.type: trace # ????? ??? ???? ??? ??.

